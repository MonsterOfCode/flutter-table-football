/// Searchable Model
///
/// A model that implements the [Searchable] interface, which provides a method to search for items in a list.
abstract class Searchable {
  /// Computed property to be used during the search process
  String get searchable;
}
